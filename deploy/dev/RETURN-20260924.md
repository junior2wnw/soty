# Soty return to dev — verified 24 September 2026

Soty serves its complete original data from `ssh dev` (`95.105.28.201`).
The Timeweb host (`94.228.125.243`) forwards Soty requests to dev over verified
TLS, including local consumers of `app:8080` and `127.0.0.1:18182`.
Its former partial application is stopped and retained. Other virtual hosts
and applications were preserved.

## Source and running release

- Repository: `https://github.com/junior2wnw/soty.git`.
- Branch: `codex/soty-return-dev-20260924`.
- Application revision: `d9585cd2285e2f2288f607c86356848a6c9cc127`.
- Image: `soty-online-chat:return-d9585cd2285e`.
- Immutable image: `sha256:caf1585535b0a19695deb96aa21ab671a6d52aa87640a972370a40a494ad6cbc`.
- Source archive on dev: `/home/ai2/soty-releases/return-d9585cd2285e`.
- Original hotpatched dev source: `e3813713a7b2fab826871a4cb8487618117b8743`,
  pushed on `codex/preserve-dev-runtime-20260924`.
- The running release includes the later `2904528` inference implementation.
  Frontend source and dependencies are unchanged from the original dev image.

## Data and configuration

The original `soty-online-chat-data` volume remains authoritative. All 1,049
original non-SQLite files remain present, including 900 room files. Six rooms
received newer encrypted snapshots after clients reconnected; these updates
were retained. No original room file is missing.

The final registry contains 15 connector registrations, 7 grants, 333 jobs,
333 inputs, 333 results, 3,589 events and 59 durable requests. Every original
identity binding and the additional Timeweb registration were verified without
printing credentials. The extra event is the terminal expiry of an old queued
job; that job was not replayed. The newly migrated connector had not polled
during the verification window; its stored binding was verified, not a live
connection from that offline client.

The ordinary seven-day cleanup ran when connector traffic resumed. The final
release enables `SOTY_CONNECTOR_PRESERVE_HISTORY=1`. Offline reconciliation
restored 332 missing terminal jobs and 14 missing registrations from the
encrypted original snapshot, preserving every newer row. A regression test
verifies that this setting retains history while expired grants remain denied,
stale queued jobs fail, uncertain executions stay assigned, and durable request
identities prevent replay. SQLite integrity and foreign keys passed.

All four current application keys authenticate. Latest inference settings match
Timeweb exactly; original dev traffic addresses and the 64 existing gateway
credentials were preserved. The gateway is healthy.

## External and functional checks

- Both authoritative DNS servers and a public resolver returned dev for the
  main domain. The `www` record also points to dev; the secondary Soty domain
  already pointed there.
- `https://xn--n1afe0b.online/ready` returns 200 via both the old and new IPs,
  with successful certificate validation.
- `https://soty.pochinit.online/ready` returns 200.
- `https://www.xn--n1afe0b.online/` returns a valid HTTPS 301 to the main domain.
- Frontend assets load; the secondary domain's browser UI rendered without
  console errors. The browser automation client blocked opening the IDN URL,
  so direct HTTPS and WebSocket checks verified that domain separately.
- Public WebSocket ping/pong passed through both server IPs.
- A neutral live model probe passed in JSON and streamed SSE modes, including
  the stream completion marker. The authenticated catalog lists three models.
- The Timeweb forwarding container and the dev traffic gateway are healthy.

Validation passed: inference self-tests, type checking, production build,
connector integration, connector store tests and all 13 persistence scenarios.
The final immutable image also passed an isolated canary against synthetic data.

## Recovery material and operating notes

Encrypted configuration and full-data snapshots are in the operator workspace
under `.codex-work/return-dev-20260924`. AES-GCM decryption was verified; the
local recovery key is protected with Windows DPAPI. Secrets, room capabilities,
database contents and snapshots are not committed to Git.

| Snapshot | Encrypted archive SHA-256 |
| --- | --- |
| Original complete dev volume | `ff97f64ff9ec9aa4a6f3c361e972d542d83cc000be47182c79ba2b5f86e1fd0b` |
| Timeweb partial volume before forwarding | `8aa828807519642c0e0d2eb6a5ec01d53fe0cccb0ebe41a4f2ca9e87d28d2570` |
| Live dev volume before history reconciliation | `e5ee38f7ee0cb5d066706f71f4614ab63539a29f3b5d487be7cf59768ee6e65b` |

Keep dev as the only writer. Do not start the retained Timeweb application or
recreate it from the old Timeweb Compose deployment: its data is incomplete.
Keep the forwarding path for cached DNS and former-host internal consumers.
Future dev recreations must preserve the live environment, existing volume,
application-token mount and history-preservation setting. Rollback must retain
the newest dev data, including changes accepted since the return.
