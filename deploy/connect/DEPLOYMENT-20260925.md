# Соты Connect — квитанция рабочей выкладки 25 сентября 2026

Connect **0.1.1** установлен на `ssh dev` и проверен через оба публичных адреса: [соты.online](https://xn--n1afe0b.online) и [soty.pochinit.online](https://soty.pochinit.online). Публичная проверка завершена 24 сентября в 22:32 UTC, 25 сентября в 03:32 Екатеринбург. Подписанный источник и постоянная служба обновления включены.

## Точные версии

| Компонент | Подтверждённое значение |
|---|---|
| Git-ветка | `codex/connect-release-20260925`, `https://github.com/junior2wnw/soty.git` |
| Приложение и host controller | `7f76096bf291d0ed3c741573d76af908f70cfbf7` |
| Адаптер резервирования | `1aa5c9273fa3eec0cbdc20de2823c3e51f55fff9` |
| Работающий image ID | `sha256:b0c58d917925faba77ac34d23dc042036741634378680f7e994baf480ad6220a` |
| Работающий container ID | `3dd39af484adb3806f2d72bd0e0ac489a948367ed9c4d0c02a039ac0bd83546a` |
| Дерево Connect | `1c96138eb59eea8b0733b06e3200f2c68f31e027c22e7a487935209575c0a50d` |
| Версия / последовательность | `0.1.1` / `1` |
| Ключ подписи | `soty-connect-2026-09`, Ed25519 |
| SHA-256 опубликованного артефакта | `fd40fdace4e1af9edcaad22930b8455b243f0faef46b83afbd5f8da19b4422df` |
| Срок принятия этого артефакта | `2026-12-23T21:48:11.002Z` |

Источник: [stable.json](https://xn--n1afe0b.online/releases/connect/stable.json). Неизменяемый выпуск: [release-1.json](https://xn--n1afe0b.online/releases/connect/release-1.json). На обоих доменах проверены точные байты, подпись по отдельно закреплённому публичному ключу, `no-store` для stable, immutable cache для номерного файла и строгие ответы 404/405 вне разрешённого GET-контракта.

## Автоматическое обновление

`soty-connect-update.timer` установлен как пользовательская systemd-служба: **enabled, active**; `Linger=yes`. Расписание — 00/06/12/18 UTC со случайным сдвигом до десяти минут. Зафиксированный следующий запуск: **25 сентября 05:03:29 +05**. Первый запуск самой службы через публичный HTTPS-источник успешно вернул:

```json
{"ok":true,"status":"current","version":"0.1.1"}
```

Главный checkout закреплён в `/home/ai2/.local/share/soty-connect/source`. Адаптер резервирования отдельно закреплён в `host-tools/1aa5c9273fa3eec0cbdc20de2823c3e51f55fff9`; его абсолютный путь указан в `config.json`. Это позволило уточнить права архиватора после штатного отката первой попытки, сохранив журнал и уже проверенный образ приложения.

Автоматически принимаются подписанные совместимые патчи `0.1.x`. Каждый выпуск проходит проверку подписи, последовательности, совместимости и сборки, затем резервирование и контролируемое переключение контейнеров. Порядок публикации следующих выпусков приведён в [README](README.md). Новый коммит в Git сам по себе не публикует подписанный релиз. До истечения срока текущего артефакта требуется следующий подписанный выпуск; истечение не выключает работающие аккаунты.

## Сохранность данных и конфигурации

- Прежний том `soty-online-chat-data`, переменные приложения, порты и секретный bind-файл сохранены; сравнение выполнялось в памяти без вывода значений секретов.
- Все **905** прежних корневых JSON-файлов присутствуют; хеш множества имён прежний. Поля авторизации всех 905 совпали. **898** файлов совпали побайтово. Семь файлов изменились во время возобновления живых соединений; их побайтовая неизменность не заявляется. Три ранее существовавших файла с невалидным JSON сохранились побайтово.
- Connector: `integrity_check=ok`, **333** jobs/inputs/results, **3589** events, **59** requests, 16 connectors и 7 access grants. Хеш полной истории jobs/inputs/results/events до и после совпал: `6eaf8fc863f010ee8a9d9559f586a3c3fee01fc3867f5f7c5491eb6882eb412f`.
- Новая база Connect прошла `integrity_check`. В браузерном профиле, созданном до выкладки, сохранились прежний ключ, имя и список комнат. Новый аккаунт и ключ устройства Connect сохранились после повторной загрузки; приватные ключи неэкспортируемы. Контрольная копия перед обновлением service worker присутствует.

Перед успешной активацией создана зашифрованная копия:

```text
/home/ai2/.local/share/soty-connect/backups/soty-2026-09-24T22-31-36-760Z-034fca73.enc
SHA-256: a439ecd5ba68b52b67ab9d3e50c764e75c5c5a9befda812265fef041522cfbee
```

Она скачана на операторский компьютер и проверена через DPAPI-ключ: аутентификация GCM успешна, архив полностью прочитан, **1067** элементов, **905** файлов комнат, одна заполненная и одна допустимая пустая служебная SQLite. Настройки и токены содержатся только внутри шифротекста. Проверка tar/заголовков SQLite не заменяет тренировку полного аварийного восстановления; целостность действующих баз проверена отдельно.

Операция активации `64644e56cf5f048796fdc95a30a04948` завершена как `admitted`, незавершённой транзакции нет. Старый контейнер `4e6aec9dbb93a26135e1882eba5f297c8984ca99bbf8256b42045f6699180cbc` сохранён остановленным с restart policy `no`. Новый работает с `unless-stopped`. Штатный откат возвращает код на последних данных и не восстанавливает старую базу поверх новых отзывов или задач.

## Проверки перед выпуском и на сайте

- Полная Linux-сборка: типы, 60 тестов Connect, прежние identity-контракты, маршрутизация моделей, durable protocol и сохранение Connector, затем frontend build.
- 43 проверки контроллера, публикации и резервирования прошли на Linux; Windows DPAPI-тест там неприменим и проверялся на Windows отдельно.
- Дополнительный настоящий Docker-тест подтвердил чтение файлов другого UID с правами 0600/0700 и запрет записи для архиватора. У него только `DAC_READ_SEARCH`, read-only mounts/rootfs и отключённая сеть.
- Изолированная пробная установка прошла. Отдельный настоящий тест намеренно отклонил готовность нового контейнера: прежний образ восстановился, запись в SQLite, сделанная **после** backup, сохранилась; остался один писатель, автозапуск неудачного контейнера отключён.
- На публичном сайте прошли десять сценариев: повторная автоматическая регистрация, контакт отдельно от входа, взаимное добавление, просмотр подтверждённого профиля, отдельные ключи устройств, зашифрованная копия, сохранение прежнего профиля, восстановление, отзыв устройства и изоляция origin.
- Публичный интерфейс проверен на ширинах **320, 390 и 1280 px**: профиль, QR контакта, устройства и сохранность. Отдельный тестовый браузер и локальный проверочный сервер закрыты; тестовые серверные экземпляры остановлены.

## Переносимый модуль

Каталог `modules/connect` остаётся независимым от инфраструктуры Сот. Готовый архив у оператора: `D:\soty-connect-release-20260925\output\SotyConnect-0.1.1.zip`. Все 28 файлов архива проверены против подписанного выпуска; SHA-256 ZIP: `fc79711265960cdfa983b10929fdbb8d001f86ccb4de3adf2353b52850c5b1f4`.

Другой проект подключает browser/server/UI и собственную активацию, хранилище и источник доверия по инструкции модуля. Аккаунты этой версии изолированы по проектам. Общий вход между независимыми доменами, passkeys и ограниченный гостевой режим телевизора остаются отдельным развитием протокола. Пользователю для восстановления после очистки браузера по-прежнему нужен второй подключённый аппарат или заранее проверенный файл восстановления.
